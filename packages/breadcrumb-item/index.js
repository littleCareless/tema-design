import TmBreadcrumbItem from '../breadcrumb/src/breadcrumb-item';

/* istanbul ignore next */
TmBreadcrumbItem.install = function(Vue) {
  Vue.component(TmBreadcrumbItem.name, TmBreadcrumbItem);
};

export default TmBreadcrumbItem;
